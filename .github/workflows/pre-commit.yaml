---
name: Pre-commit
on:
  pull_request:
  push:
    branches: [main]
  # Run this on every release so new release can have
  # updated version
  release:
    types:
      - published
  # on demand
  workflow_dispatch:

env:
  HOMEBREW_BREWFILE: ${{ github.workspace }}/Brewfile
  HOMEBREW_BUNDLE_MAS_SKIP: true
  HOMEBREW_BUNDLE_WHALEBREW_SKIP: true
  HOMEBREW_NO_INSTALL_CLEANUP: true

jobs:
  auto-update:
    name: Update pre-commit hooks
    runs-on: macos-latest
    steps:
      - name: Set up git repository
        uses: actions/checkout@v3

      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@master

      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.19

      - name: Install shfmt
        run: go install mvdan.cc/sh/v3/cmd/shfmt@latest

      - name: Install brew dependencies
        run: brew install shellcheck rcmdnk/file/brew-file pre-commit

      - name: Remove disabled forumulae
        uses: Homebrew/actions/remove-disabled-formulae@master

      - name: Setup action Brewfile and removing problematic pre-installed artifacts
        run: |
          mv .github/Brewfile.action ./Brewfile
          # For black
          rm /usr/local/bin/2to3-3.11
          # For aws
          rm /usr/local/bin/aws
          rm /usr/local/bin/aws_completer
          # For go
          rm /usr/local/bin/go
          rm /usr/local/bin/gofmt

      - name: Run pre-commit autoupdate action
        uses: browniebroke/pre-commit-autoupdate-action@main

      - name: Create Pull Request with updates
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.BOT_TOKEN }}
          base: ${{ github.head_ref }}
          branch: update/pre-commit-hooks
          title: Update pre-commit hooks
          commit-message: "chore: update pre-commit hooks"
          body: Update versions of pre-commit hooks to latest version.
